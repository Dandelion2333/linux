一、权限问题：
	1、提示mv的时候权限不够，则需要给权限即可
	sudo mv file0 dir0
	
二、指令相关：
	1、在终端中复制与粘贴，分别为：
		Ctrl+shift+c
		Ctrl+shift+v
		
	2、再vim中不小心使用了Ctrl+S后，整个界面像是卡死了一样。原因是禁止输入了。可以使用Ctrl+Q解除禁止。
	
	3、删除路径下的所有文件，可以用rm * 正则表达式的方式
	